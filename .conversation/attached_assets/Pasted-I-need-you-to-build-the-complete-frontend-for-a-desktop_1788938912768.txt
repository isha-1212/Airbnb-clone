I need you to build the complete frontend for a desktop-only Airbnb listing clone.

I am providing:
1. REFERENCE_SPEC.md
2. IMPLEMENTATION_PLAN.md
3. Reference screenshots
4. Reference URL:
https://airbnb-clone-umber-two.vercel.app

IMPORTANT:
The reference URL and screenshots are visual/behavioral references only.
Do NOT copy, scrape, extract, reproduce, or lift the reference site's source code, bundled JS, React components, or implementation.
Implement the UI independently from the provided specification and screenshots.

SOURCE OF TRUTH:
- Use REFERENCE_SPEC.md for the detailed visual and behavioral specification.
- Use IMPLEMENTATION_PLAN.md for the implementation structure.
- Use the supplied screenshots for visual verification.
- If the screenshots and spec conflict, prioritize the screenshots for visual appearance and the corrected behavior documented in the spec.

TECH STACK:
- React
- Vite
- TypeScript
- CSS / CSS Modules
- React hooks
- IntersectionObserver where appropriate
- No backend
- No database
- No Tailwind
- No Redux
- Avoid unnecessary dependencies

SCOPE:
Desktop only. Do NOT spend time implementing mobile or tablet layouts.

REQUIRED VIEWS:
1. Listing Page
2. Photo Tour
3. Lightbox

IMPORTANT VISUAL REQUIREMENT:
This is a pixel-accurate recreation task.
Do not redesign the interface.
Do not introduce your own visual style.
Match:
- spacing
- typography
- font weights
- borders
- radii
- shadows
- image aspect ratios
- section widths
- alignment
- button dimensions
- icon placement
- sticky behavior
- scroll behavior
- visual hierarchy

SECONDARY NAVIGATION:
Pay special attention to the documented corrected behavior:
- It is initially in normal document flow below the hero/title area.
- It does NOT start fixed to the viewport.
- Once it naturally reaches the top of the viewport, it becomes sticky.
- It becomes a full-width horizontal white bar with a bottom border.
- Its inner content follows the page's horizontal content margins.
- Left side contains:
  Photos | Amenities | Reviews | Location
- Right side contains:
  compact price | rating/reviews | Reserve
- Active underline changes according to the currently visible section.
- Preserve the exact behavior documented in REFERENCE_SPEC.md.

IMPLEMENTATION:
First inspect the provided files and screenshots carefully.
Then create the complete application.

Do NOT stop after creating a skeleton.
Implement the full long Listing Page, Photo Tour, and Lightbox according to the provided materials.

Use reusable components and data-driven rendering where appropriate, but do not over-engineer.

Use placeholder/local assets only where actual reference assets are not available. Do not use random images that materially change the visual appearance.

INTERACTIONS:
Implement the interactions explicitly documented in the specification, including:
- Photo Tour navigation
- Opening photos in Lightbox
- Lightbox navigation
- Close behavior
- Secondary navigation scrolling
- Sticky navigation
- Active section synchronization
- Reserve/button interactions where specified
- Any other clearly documented interaction

After implementation:
1. Run the application.
2. Run the production build.
3. Fix build/runtime errors.
4. Check all required views.
5. Check scrolling and sticky navigation.
6. Perform a visual polish pass against the supplied screenshots.
7. Remove obvious placeholder/debug UI.

Do not create the architecture diagram yet.
Do not create unnecessary backend functionality.

Start implementing now.